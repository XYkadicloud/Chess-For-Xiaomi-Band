# Chess Vela 0.2.8 优化与对比说明

日期：2026-09-22

## 结论

启动烟雾测试包可以在小米手环 9 原机正常打开，因此基础路由、setup 页面和最小 game 页面没有导致重启。问题集中在正式棋局页创建时的页面复杂度和资源峰值。本版本包名改为 `com.xykadi.chess`，版本为 `0.2.8`，versionCode 为 `10`。

## 与原始 0.2.2/0.2.3 归档的对比

项目文件资源归档中的旧版本 manifest 实际标记为 `0.2.3`，不是 0.2.2。旧版棋局页使用 `data`、`this.$page` 和启动阶段多次 storage 读取，且首屏同样创建 64 个棋盘格和多层 overlay；因此不能直接回退旧版。旧版的 PNG 棋子资源本身很轻，12 张 PNG 总大小约 6 KB，不是本次重启的主要原因。

本次保留了旧版的 PNG 棋子资源，并把它们恢复到 `src/common/pieces/`。没有改成 SVG，也没有删除棋子图片。

## 0.2.8 修改内容

首页删除 active-game storage 读取，点击“开始游戏”始终直接进入 setup，避免旧暂停数据和异步回调改变启动路径。首页按钮改用 `touchend`。

棋局页删除棋盘坐标层，棋盘格不再创建独立的 selected 和 lastMove 子节点，而是在 64 个格子的样式属性中直接合并背景色和边框。PNG 棋子 image 节点继续保留，因此棋盘外观和棋子显示不会被移除。

棋局页仍使用一个父容器处理棋盘触摸，避免 64 个格子绑定独立 click。启动阶段继续不读取 storage，历史和 PGN 仍不参与首次棋局初始化。

## 验证

```text
Vela source checks: PASS
Active UX scripts checked: 6
Chess state checks: board=64, undo=0, cache=reused
AIoT Toolkit build: SUCCESS
Package: com.xykadi.chess
Version: 0.2.8 / 10
```

## 真机测试

安装新包前建议卸载旧的 `com.codex.test.chess`，再安装 `com.xykadi.chess.debug.0.2.8.rpk`。重点确认首页进入 setup、选择时间、进入棋盘、棋子图片显示和第一步落子。

如果 0.2.8 仍然重启，下一步不再继续在单页上堆功能，而是制作正式的分层棋局页：首屏只保留棋盘、PNG 棋子、回合和时钟；菜单、PGN、历史和设置改为延迟页面，逐项加回并在真机验证。
