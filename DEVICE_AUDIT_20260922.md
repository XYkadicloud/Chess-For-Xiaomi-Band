# Xiaomi Band 9 实机启动问题审计

日期：2026-09-22

## 审计结论

当前 0.2.7 源码已通过 UX 脚本语法检查、模板编译和现有棋规自检。AIoT Toolkit 能够成功生成 `game.ux` 的编译产物，且生成代码明确允许 `private` 与 `protected` 同时存在；因此当前版本没有发现一个可以直接判定为“必然语法错误”的问题。

不过，实机重启仍然高度可能来自棋局页的资源峰值和页面复杂度，而不是单一棋规函数。当前棋局页在第一次渲染时同时包含 64 个动态棋盘格、每格的条件子节点、31 个模板事件绑定、多个全屏 overlay、PGN 列表、图片资源和完整棋规方法对象。源码审计还发现棋局页保留 8 次 storage 调用和一处 `JSON.parse(JSON.stringify(positionCounts))` 深拷贝，虽然它们不在 0.2.7 的首次 `onInit` 路径中，但会增加页面 VM 和后续操作的风险。

## 已确认的代码事实

| 检查项 | 结果 |
|---|---|
| UX 脚本语法 | 6 个活动页面通过 |
| manifest JSON、路由和能力 | 通过；当前能力与 import 一致 |
| `data` 与 `protected` 冲突 | 未发现；game 使用 `private + protected`，编译器接受 |
| setup → game 路由 | 编译产物确认路由和 touchend 事件已生成 |
| 棋局初始棋盘 | 64 格、初始白兵合法走法测试通过 |
| 当前 game 页模板事件 | 31 个 |
| 当前动态棋盘格 | 64 个 |
| game 页 storage 调用 | 8 个，均为菜单/保存/退出路径，不是 onInit 必需调用 |
| game 页深拷贝 | 1 处，保存历史记录时复制重复局面表 |
| app.ux | 空生命周期，无额外系统调用 |

## 风险排序

### 高风险：棋局页首次创建过重

`game.ux` 同时拥有棋盘、计时、菜单、设置、升变、PGN 和历史保存逻辑。即使 `onInit()` 不再读取 storage，Vela 运行时仍需创建完整页面 VM、解析所有模板和生成棋盘节点。模拟器内存充足时可以正常运行，Band 9 真机可能在页面创建/首次 diff 阶段重启。

### 中风险：首页 active-game 异步恢复

首页 `onShow()` 仍会读取 `CHESS_ACTIVE_GAME`。如果旧数据残留且包含 `resumeId`，唯一的“开始游戏”会直接路由到 game 页，而不是 setup。0.2.7 的 game 页已经不再真正恢复该记录，但首页仍然把旧状态作为入口条件。这会让真机测试路径不确定，也会把旧数据带入页面参数。

### 中风险：棋局页保留历史/PGN存储逻辑

历史保存包含整盘 `historyStack`、棋盘、PGN、重复局面表，且有 storage get → parse → set 的链路。这不是首次启动的直接原因，但在实机上进行菜单操作、退出或暂停保存时可能造成峰值和卡顿。

### 低风险：坐标和触摸兼容性

当前 `onTouchEnd()` 使用固定 `pageY - 74` 计算棋盘坐标。它不会解释启动重启，但在不同状态栏/设备布局下可能导致落子位置偏移。当前模板编译器已经确认 `touchend` 事件确实被生成。

## 建议方案

### 阶段 A：独立启动烟雾测试

制作一个独立包，保留首页和 setup，只把 game 页替换为不含棋盘、不含 storage、不含计时器的极简页面，并使用不同 package 名以便与正式版共存。

- 若烟雾测试包仍然进入 game 后重启：优先判定为真机路由、页面参数或 Vela 运行环境问题。
- 若烟雾测试包稳定打开：可确认正式 game 页的页面体积/渲染峰值是主因。

### 阶段 B：正式版拆分棋局页

将 game 页改为低资源版本：

1. 首屏只保留棋盘、回合和两个时钟；菜单、设置、PGN、升变用独立页面或延迟渲染。
2. 将棋盘格从 64 个复杂动态节点减少为静态背景加少量棋子节点，或使用单张棋盘图层。
3. 首屏关闭走法提示，首次点击棋子后才计算合法走法。
4. 删除启动路径中的历史/PGN/暂停恢复代码，待基础对局稳定后单独恢复。
5. 将 8 次 storage 调用全部移到非启动页面；每次保存限制数据长度。
6. 删除 `JSON.parse(JSON.stringify(positionCounts))`，使用有界增量快照。
7. 去除非标准 `global.runGC` 依赖，仅做生命周期引用释放。

### 阶段 C：真机验收矩阵

每次只测试一个变量：

1. 极简 smoke game；
2. 只有静态棋盘；
3. 静态棋盘 + 棋子；
4. 棋子选择；
5. 合法走法提示；
6. 计时器；
7. 菜单和 storage。

这样没有日志也能定位是哪一层使设备重启。

## 当前判断

基于编译成功、模拟器正常、真机进入棋局重启以及当前页面规模，**性能/内存峰值的可能性高于 JS 语法错误**。但在没有烟雾测试包结果之前，不能把它说成百分之百；阶段 A 是成本最低且信息量最大的下一步。
