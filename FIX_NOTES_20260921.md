# Chess Vela 0.2.7 修复与自检说明

日期：2026-09-21

## 结论摘要

本轮已针对 Xiaomi Band 9 Vela 快应用的首页重复入口、进入棋盘不稳定、恢复棋局参数丢失、无限制棋局以及小米手环 9 实机低内存和触摸交互问题完成修改。源码版本已更新为 `0.2.7`，版本号为 `9`。

### Band 9 实机低内存优化

0.2.7 暂时冻结历史棋局和 PGN 保存/恢复在启动阶段的参与。棋局页不再等待任何 storage 回调，直接读取路由中的 `minutes`、`unlimited`、`boardSize` 并调用 `newPosition()`。开始对局页移除 storage 读写，选项和开始按钮统一使用 Vela 原生 `touchend`；棋盘移除 64 个格子的独立 click 监听，改为 `boardViewport` 单一触摸分发，在 touchend 时根据坐标计算格子。这样可以减少弱设备上的异步启动、原生事件对象和页面绑定开销。

### 追加确认的进入棋局崩溃根因

通过检查 AIoT Toolkit 生成的 `build/pages/game/game.js`，确认运行时包装器明确禁止同一个页面 VM 同时声明 `data` 与 `protected`，并会直接抛出“页面 VM 对象中的属性 data 不可与 data、protected、private 同时存在”的异常。此前 `game.ux` 同时使用了 `data` 保存内部棋局状态、`protected` 接收路由参数，因此 setup 页面可以打开，但进入 game 页面时会在 VM 创建阶段失败。现已将 `game.ux` 和 `historyDetail.ux` 的内部状态改为 `private`，路由参数继续保留在 `protected`。本次 0.2.6 RPK 已在该修复后重新构建。

## 为什么首页会出现两个相同功能

旧版 `src/pages/index/index.ux` 同时提供了三种入口状态：无暂停棋局时显示“开始游戏”，有暂停棋局时显示“继续对局”，并且无论是否存在暂停棋局始终显示“新建对局”。在没有暂停棋局的正常状态下，“开始游戏”和“新建对局”都执行相同的 `router.push({ uri: '/pages/setup' })`，因此它们是重复功能。

本轮将主页入口收敛为一个固定的“开始游戏”按钮。若存在暂停棋局，该按钮内部自动进入恢复流程，并在按钮下方显示一行非交互提示“有一局暂停棋局，点击后继续”，不再增加第二个操作按钮。

## 为什么点击后可能表现为重启

在没有日志的情况下，无法把设备的系统重启与某一条运行时异常做百分之百对应；但代码中发现了两个明确的稳定性风险，它们都已经修复：

1. **页面参数读取方式不符合 Vela 规范。** 旧版 `game.ux` 通过 `this.$page.minutes` 和 `this.$page.resume` 读取路由参数。Vela 官方规范要求在页面的 `protected` 中声明参数，再从 `this.minutes`、`this.resume` 读取。设备运行时若没有提供 `$page` 对象或对象初始化时序不同，进入棋盘时会落入异常路径。现在已声明 `protected: { minutes: '10', resume: '' }`，并在初始化阶段做安全默认值处理。历史详情页的 `id` 参数也同步修复。

2. **棋局启动阶段有多段异步存储回调，且没有统一的异常兜底。** 旧版先读棋盘尺寸，再读设置，再决定新局或恢复；若某个存储 API 在设备上同步抛错、重复回调或返回格式不同，初始化可能中断。现在增加了一次性启动闸门、存储 API 的 `try/catch` 和新局兜底，确保无论启动配置是否存在都能调用 `newPosition()` 完成 64 格棋盘初始化。

另外，旧版在 `app.ux` 的应用级 `onCreate/onShow` 中无条件调用常亮 API。该调用本身不一定是重启根因，但会把设备能力差异暴露在每次应用启动路径上。本轮改为仅在棋局页按需开启常亮，并在棋局页销毁时关闭；调用同时加了失败保护。

## 性能与内存优化

- **合法走法缓存改为状态缓存。** 旧版以完整局面字符串加棋子索引作为动态对象键；新版使用 `{ state, moves }`，同一局面只保留 64 个格子的按需结果，走子后整体失效，减少键字符串和重复对象。
- **撤销历史设置上限。** `MAX_UNDO = 60`，避免长时间对局无限增长。
- **撤销快照不再深拷贝完整重复局面表。** 旧版每一步使用 `JSON.parse(JSON.stringify(positionCounts))`，长局会产生大量重复对象。新版只保存上一步局面键及其计数，撤销时增减对应计数；历史记录恢复仍兼容旧格式。
- **拖动节流定时器可回收。** 棋盘拖动的 `setTimeout` 现在保存到 `touchTimerId`，触摸结束或页面销毁时清理，避免页面离开后回调继续修改状态。
- **存储写入增加并发保护。** 新增 `saveInFlight`，防止快速连续点击暂停、保存棋谱或退出时并发写同一个历史键。
- **生命周期资源清理。** 游戏页 `onDestroy` 中停止时钟、清理拖动定时器、关闭常亮，并在设备提供 `global.runGC` 时尝试触发回收。
- **减少路由和系统能力声明。** 当前项目定位为离线单设备双人棋局，清单只保留实际使用的主页、设置、棋盘和历史页面，以及 `router/app/brightness/storage` 能力；遗留的搜索、网页阅读和旧菜单源码仍保留在源码目录中作为备份，但不再进入当前路由表。

## 其他一并修复

- 历史详情页恢复棋局时传递原始 `whiteMinutes`，不再默认错误地回到 10 分钟。
- 退出对局和暂停保存使用带 `clearTask` 的主页跳转，避免返回到已经结束的 setup 页面形成页面栈堆积。
- 菜单中的“新建棋局”和“保存并再来一局”会清除旧的暂停指针，避免主页误把旧棋局当成可恢复对局。
- 开始对局页的时间选中态改为跟随 `timeControl`，不再固定高亮 20 分钟。
- 开始游戏与开始下棋按钮都增加了防重复点击保护。

## 当前验证结果

已在沙盒中运行 `node tools/verify_vela_source.js`，结果为：

```text
Vela source checks: PASS
Active UX scripts checked: 6
Chess state checks: board=64, undo=1, cache=reused
```

该检查覆盖：

- `manifest.json` JSON 解析、版本号和当前路由集合；
- 6 个实际路由页面的 `<script>` 语法解析；
- 首页只有一个“开始游戏”，没有“新建对局”和旧 `startNew` 处理器；
- 棋盘 64 格初始化；
- 初始白兵合法走法 `e2-e3/e4`；
- 同一局面合法走法缓存复用；
- 一次落子后的棋盘、回合和撤销快照；
- 撤销快照不再包含完整 `positionCounts` 深拷贝。

本次已使用 GitHub 仓库中的 `package.json`、`package-lock.json` 和 `aiot-toolkit 2.0.4` 完成正式 RPK 构建。首次构建发现仓库中残留的旧 `system.interconnect` 桥接代码与当前离线 manifest 不一致；随后仅在临时构建目录中排除了未使用的旧搜索、网页阅读、旧菜单页面和桥接脚本，构建成功。当前项目根目录已补回构建配置，生成的安装包位于 `dist/com.codex.test.chess.debug.0.2.7.rpk`。

## 真机验收顺序

1. 卸载旧版本，再安装 `0.2.7` RPK，避免旧页面缓存。
2. 从主页点击唯一的“开始游戏”，确认只进入一次 setup，不回到主页或重启。
3. 选择 3、5、10、15、20 分钟分别开始，确认棋盘页能稳定显示。
4. 在设置中切换标准、放大、紧凑三种棋盘尺寸，重新开始确认启动尺寸正确。
5. 连续点击“开始游戏”和“开始下棋”，确认不会重复创建页面。
6. 下满 4 步后执行“暂停保存”，回到主页后再次点击“开始游戏”，确认棋盘、回合、计时和棋谱恢复。
7. 在棋局菜单执行“认输”“和棋”“退出对局”，确认计时停止、棋盘锁定且不会回到旧 setup 页面。
8. 进行长局拖动和快速选子，观察是否出现明显卡顿、触摸结束后仍移动或页面闪退。
